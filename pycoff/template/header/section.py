def RawData(file, json_data, py_data):
    # tell = file.tell()

    # file.seek(json_data["#ArchiveMemberHeader"] + json_data['PointerToRawData'])
    # data = file.read(json_data['SizeOfRawData'])
    # file.seek(tell)

    # return data
    # print(file.read(json_data))
    pass
